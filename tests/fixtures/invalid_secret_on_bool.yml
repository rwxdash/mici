# @test: validate should FAIL
# @expect-errors:
#   - mici::schema::secret_requires_string  (secret: true on a boolean input)
# @note: Only "string" type inputs can be marked as secret

version: "1.0"
name: "secret-bool"

configuration:
  confirm: false

inputs:
  force:
    type: boolean
    description: "A boolean that shouldn't be secret"
    secret: true

steps:
  - id: "noop"
    run:
      command: "echo noop"
